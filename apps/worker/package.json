{
  "name": "@aervox/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aervox/agent-loop": "workspace:*",
    "@aervox/config": "workspace:*",
    "@aervox/database": "workspace:*",
    "@aervox/diary": "workspace:*",
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.39.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "7.0.2"
  }
}
